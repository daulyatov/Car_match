services:
  - type: web
    name: carmatch
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: "gunicorn CarMatch.wsgi:application"
    staticPublishPath: staticfiles 